# 32+ characters; rotate quarterly. Generate with: openssl rand -base64 48
SESSION_PASSWORD=dev-only-change-me-this-must-be-32-chars-min

# Mock client base latency (ms). Default 250.
MOCK_LATENCY_MS=250
